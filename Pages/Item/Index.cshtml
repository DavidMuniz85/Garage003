@page
@model Garage003.Pages.Item.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Items</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                SKU
            </th>
            <th>
                Category
            </th>
            <th>
                Current Status
            </th>
            <th>
                Zone
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach(var item in Model.Item) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemSKU)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status.StatusName)
                    @if (item.ItemsZones.Count == 0)
                    {
                        <td>
                            Unassigned
                        </td>
                    }
                    else
                    {
                        @foreach( var zone in item.ItemsZones)
                {
                    <td>
                        @Html.DisplayFor(modelItem2 => zone.Zone.ZoneName)
                    </td>
                }

                    }
            </td>
                
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ItemId">Edit</a> |
                <a asp-page="./Delete" asp-route-id="@item.ItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
